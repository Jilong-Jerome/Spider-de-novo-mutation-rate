# 06 · Mutation rate & callable genome

- `callable_depth/` — per-individual callable-genome estimation from sequencing
  depth (see `coverage_report/README.md`).
- `callable_genome_viz/` — visualization of callable genome size across species
  (`plot_callable_genome.py`, `callable_genome_summary.tsv`).
- `supplement_tables/` — mutation-rate supplementary tables (`Table_S1.tsv`,
  `write_S1.py`).
