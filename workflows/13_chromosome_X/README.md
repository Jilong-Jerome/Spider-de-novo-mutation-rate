# 13 · Chromosome X

gwf workflow (entry point `workflow.py` with `workflow_templates.py`,
`workflow_targets.py`).

Per-chromosome and X-specific sequencing-depth statistics and visualization
(`stat_per_chrom.py`, `depth_per_chrom.R`, `depth_viz.R`), used to handle the X
chromosome separately in mutation-rate estimation.
`offsprings_not_scanned.tsv` lists offspring excluded from scanning.
