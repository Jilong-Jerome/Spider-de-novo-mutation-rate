# 09 · Male mutation bias (alpha)

- `DNM_alpha/` — estimation of the male mutation bias α from parent-of-origin
  resolved DNMs (gwf workflow `workflow.py` + `workflow_sources/`,
  `configurations/`).
- `parental_bias_unique_shared/` — parental-origin assignment and unique vs.
  shared DNMs among siblings (`scripts/`, `results/`; see
  `results/methods_supplement.md`).
