# 12 · Kinship & trio verification

- `kinship_check/` — VCF-based relatedness/kinship checks (gwf workflow;
  `vcf_filter_maf_snps.py`, `vcf_fill_id.py`, PLINK).
- `kinship_identification/` — confirmation of trio identity / parentage
  (gwf workflow; per-species `configurations/*.config.yaml`; see `README.md` and
  `METHODS_trio_check.md`).
