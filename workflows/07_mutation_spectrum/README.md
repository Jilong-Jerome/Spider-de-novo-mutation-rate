# 07 · Mutation spectrum

- `mut_spectrum/` — germline mutation spectrum from the trio DNM call set
  (gwf workflow; `mut_class_check.py`).
- `DNM_spectrum_compare/` — comparison of germline vs. somatic spectra and somatic
  mutation rate estimation (see `README.md` and
  `docs/somatic_mutation_rate_methods.md`).
