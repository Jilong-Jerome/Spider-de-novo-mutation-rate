# 13 · Site frequency spectrum & runs of homozygosity

gwf workflow (entry point `workflow.py` with `workflow_templates.py`,
`workflow_targets.py`).

- Site frequency spectrum from population VCFs (`process_vcfs.sh`, ancestral-allele
  VCF lists such as `DUM_aa_vcfs.txt`, sample lists `DUM_inds.txt`).
- Runs of homozygosity calling (`ROH_call.py`).
